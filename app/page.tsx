import {ArrowUpRight,ArrowDown,Code2,MoveUpRight} from 'lucide-react';
import Orbit from './orbit';
import Gallery from './gallery';
const github='https://github.com/mattvildibill/';
function External({href,children,className=''}:{href:string;children:React.ReactNode;className?:string}){return <a href={href} className={className} target="_blank" rel="noopener noreferrer">{children}<ArrowUpRight size={16}/><span className="sr-only"> (opens in a new tab)</span></a>}
export default function Home(){return <><a href="#work" className="skip-link">Skip to projects</a><header className="site-header"><a className="identity" href="#top"><span className="monogram">mv</span><span>Matt Vildibill<span className="identity-role">Software engineer</span></span></a><nav aria-label="Main navigation"><a href="#work">All six projects</a><a href="#approach">Engineering notes</a><External href={github+'matt-vildibill-portfolio'} className="nav-source">Portfolio source</External></nav></header><main id="top"><section className="hero"><div className="hero-copy"><p className="eyebrow">SOFTWARE / SIMULATION / EXPLORATION</p><h1>Complex systems.<br/><span>Made tangible.</span></h1><p className="intro">I build interactive software for exploring how things work—from network fabrics and orbital motion to race telemetry and walkable worlds.</p><a className="primary-link" href="#work">Explore the work <ArrowDown size={18}/></a><div className="hero-footnote"><span className="short-rule"/> Selected projects, with the code behind them.</div></div><Orbit/></section><div className="discipline-strip"><span>01 / Numerical simulation</span><span>02 / Data interfaces</span><span>03 / Explorable environments</span></div><section className="work-section" id="work"><div className="section-heading"><div><p className="eyebrow">SELECTED WORK</p><h2>Open it. Explore it.<br/>Look under the hood.</h2></div><p>Six projects. Six ways in.<br/>Choose a project, then interact with the real application.</p></div><Gallery/></section><section className="approach" id="approach"><div><p className="eyebrow">ENGINEERING NOTES</p><h2>The interesting part<br/>is how it behaves.</h2></div><div className="approach-text"><p>Simulation, visualization, and data-heavy interfaces share a problem: the user needs to see what a system is doing—and understand what the model can actually tell them.</p><p>These projects make that behavior inspectable through controls, comparisons, and explicit model boundaries. The source links go straight to the implementation.</p><p className="provenance">Project descriptions are grounded in implementation source. Each application includes its own model boundaries; the portfolio also links to the bundled demo code.</p></div></section></main><footer><a href="#top" className="footer-name">Matt Vildibill <MoveUpRight size={25}/></a><span>Software, made explorable.</span><External href={github+'matt-vildibill-portfolio'}>Built in the open</External></footer></>}
